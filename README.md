# easy-text-encryptor
EasyTextEncryptor is a simple PHP tool for encrypting and decrypting text. It uses a custom encryption method with dynamic key hashing for each block and adds a signature for data integrity. The encrypted text is base64 encoded for easy storage or transmission. Ideal for basic encryption needs.
